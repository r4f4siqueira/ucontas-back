# Dentro da pasta raiz (ucontas-back)
# nest: cli usada
# g: parametro de generate que serve para gerar um arquivo ou algo
# controller: oque será gerado
# users: nome do controller ou no caso o modulo, passando somente users ele gerou um arquivo users.controller.ts e modificou o modulo userns.module, caso não tivesse o módulo ele criaria
$nest g controller users

# Na duvida use
# Mostra todos os comandos e opções do CLI do NEST
$nest --help